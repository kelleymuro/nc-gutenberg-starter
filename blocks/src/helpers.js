class Helper {
  static getProductPrice(product) {
    //const productPrice = () => {

    //check if north_commerce_settings is defined
    //if (typeof north_commerce_settings === 'undefined') return product.base_price;

    //check if currency is defined
    //if (typeof north_commerce_settings.currency === 'undefined') return product.base_price;

    //check if currency symbol is defined
    //if (typeof north_commerce_settings.currency.symbol === 'undefined') return product.base_price;

    //};
    return "0€";
  }
}

export default Helper;
